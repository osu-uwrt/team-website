$(document).ready(function(){
  $('.slide-renders').slick({
    autoplay: true,
    autoplaySpeed: 3000,
    arrows: false,
    cssEase: 'linear',
    dots: true,
    fade: true,
    infinite: true,
    speed: 600
  });
});
